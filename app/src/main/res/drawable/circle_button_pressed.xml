<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <stroke
        android:width="1dp"
        android:color="@color/vif_dark" />
    <solid android:color="@color/button_enabled" />
    <size
        android:width="40dp"
        android:height="40dp" />
    <!--
        <stroke android:width="2dp" android:color="@color/primary_light" />
    -->
</shape>